{% extends "@Admin/Default/back.html.twig" %}

{% block body %}


    <div class="bs-example widget-shadow" data-example-id="contextual-table">
        <h3> Les Ratings :</h3>
        <table class="table">
            <thead>
            <tr >
                <th>Note </th>
                <th>Description</th>
                <th>Evenement</th>
                <th>Membre</th>
                <th>Répondre par email</th>



            </tr>
            </thead>
            <tbody>

            {% for d in demande  %}


                    {% if d.value < 2 %}
                        <tr class="danger">

                    {% elseif d.value >=2  %}
                        <tr class="success">
                        {% else %}
                        <tr class="info">
                    {% endif %}


                        <th >{{ d.value }}/5</th>
                        <th >{{ d.description }}</th>
                        <th >{{ d.ide.nomEvent }}</th>
                        <th>{{ d.idu.username }}</th>
                <td><a href="{{ path('event_mail',{'id':d.id}) }}" onclick="return confirm('envoyer un mail ?')"><i class="fa fa-envelope"></i></a></td>



                </tr>

            {% endfor %}
            <tr>

            </tbody>
        </table>

    </div>




{% endblock %}
